package com.grinwich.sdk.feature.core

/**
 * Neutral identity of the Core feature for registering contributions in
 * registries keyed by feature (e.g. [com.grinwich.sdk.contracts.ServiceRegistry]
 * from pattern E). Replaces the use of `CoreProvisions::class.java` as key.
 *
 * Intentionally empty — exists only as a type-safe identity.
 */
object CoreFeatureId
